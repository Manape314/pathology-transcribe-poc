__pycache__/
*.pyc
venv/
.venv/

# Reference terminology data (LOINC / SNOMED CT / NHLS) — large, licensed,
# and not meant to live in git. Load it from disk at runtime instead.
Medical_Terminologies/

# Generated by backend/scripts/build_terminology_index.py — derived from the
# above, regenerate locally rather than committing.
backend/data/
