# Web framework + ASGI server
fastapi>=0.110
uvicorn[standard]>=0.29

# Required by FastAPI to parse multipart/form-data file uploads
python-multipart>=0.0.9

# Whisper inference via CTranslate2. Pulls in ctranslate2, tokenizers,
# onnxruntime (used by the Silero VAD filter), and av (PyAV) for decoding.
faster-whisper>=1.0.0

# Fuzzy string matching of individual "tests required" items against the
# NHLS/LOINC controlled vocabularies (see backend/matching.py and
# backend/terminology_normalize.py).
rapidfuzz>=3.6

# Test suite (backend/tests/) — not needed to run the app itself.
pytest>=8.0
